# Codex 项目协作规则

> 适用范围：整个仓库。子目录存在 `AGENTS.md` 时，必须同时遵守离目标文件最近的局部规则。
> 目标：以最小必要上下文完成可验证的纵向业务切片，避免把整套项目文档注入每个任务。

## 1. 项目定位

本项目是二手非标商品交易的多租户 SaaS，采用 pnpm + Turborepo：

- `apps/core`：NestJS 模块化单体。
- `apps/gateway`：租户、鉴权、套餐、配额、RBAC、限流与计量入口。
- `apps/pc-admin`：PC 平台运营端。
- `apps/miniapp`：消费者小程序基线。
- `apps/merchant-miniapp`：商家小程序。
- `packages/contracts`：前后端共享契约的唯一来源。
- `packages/ui-kit`：无业务语义的设计 Token 和基础组件。

业务执行以 `docs/前后端同步开发与验收规划.md` 为主计划，以 `plans/current.md` 为当前迭代入口。

## 2. 上下文加载规则

开始任务时按顺序执行：

1. 读取本文件。
2. 读取 `plans/current.md`；若用户指定任务卡，再读取对应 `plans/backlog/*.md`。
3. 根据目标路径读取最近的局部 `AGENTS.md`。
4. 按 `docs/context-map.md` 只读取与任务直接相关的架构章节。
5. 检查目标模块现有实现、测试和未提交修改后再动手。

禁止默认全量读取以下长文档：

- `docs/多租户电商SaaS平台架构设计.md`
- `docs/codex-ai-development-spec.md`
- `docs/前后端同步开发与验收规划.md`

仅在架构决策、规范审计、规则冲突或任务卡明确要求时读取对应章节。前端视觉任务额外读取 `.trae/rules/design_system.md`。

## 3. 全局架构红线

1. Contract First：跨端字段、枚举、错误码和状态机先修改 `packages/contracts`。
2. Tenant First：租户身份来自可信请求上下文，禁止信任 Body 中的 `tenantId`、`userId` 或角色。
3. 依赖抽象：Repository、Tenant Context、Billing、Event Bus、支付、媒体和物流均通过 Port/Adapter 边界。
4. 跨模块禁止直接 import 其他模块的具体 Application Service。
5. 写操作必须评估 Feature、Quota、租户/对象授权、幂等或乐观锁、事务、审计、用量与领域事件。
6. Prisma 只能出现在基础设施层；金额使用 Decimal，时间使用带时区类型，业务数据必须具备租户隔离。
7. 前端禁止直接 `fetch`/`axios`，只能通过 API 层并复用共享契约。
8. TypeScript 严格模式；禁止新增 `any`、`@ts-ignore`、空操作和伪造成功路径。

详细规则的权威基线仍为 `docs/codex-ai-development-spec.md`；本文件是日常任务的精简执行入口。

## 4. 工作单元与计划

默认一个对话只处理一个可独立验收的功能纵向切片。任务按用户功能划分，不按后端、前端或测试阶段拆分；同一张卡必须给出商家小程序、消费者小程序、PC 平台端的实现或“不受影响”验证结论，并完成 Contract、Core/Gateway 到三端的真实闭环。开始实现前维护 3～6 步计划，同一时间只允许一个步骤为进行中。

### 用户验收闸门

- 任何页面、交互或业务逻辑只能推进到“待用户验收”。
- 用户没有明确确认验收通过前，禁止标记完成，禁止开始依赖它的下一功能。
- 文件存在、构建通过、自动测试通过、截图或 AI 自检都不能替代用户验收。
- 用户验收发现问题时，只修复当前功能并重新提交验收，不得顺带推进后续任务。

任务卡至少包含：

- 业务结果与套餐价值。
- 范围和明确的不在范围。
- 依赖与风险。
- Contract、Tenant、Feature、Quota、Audit/Event 判断。
- 商家端、消费者端、PC 平台端各自的行为、权限和验证证据。
- 可执行的验收标准与验证命令。

发现范围外问题时记录到 backlog，不顺手扩展当前任务。任务完成后更新任务卡和 `plans/current.md`；有效决策写入代码、文档或 ADR，不依赖聊天历史保存。

## 5. 修改与验证

- 保留用户已有未提交修改，禁止覆盖、回退或格式化无关文件。
- 优先做目标模块的定向测试、类型检查和 lint；只有跨包或发布前才运行全仓 `pnpm check`。
- 数据库变更必须同时验证空库部署和已有库升级路径，并提供租户越权测试。
- 完成意味着实现、相关测试、文档和环境变量示例一致；不能把“代码已写”视为完成。

## 6. 完成汇报

最终只汇报：

1. 已交付的业务结果。
2. 关键文件和重要决策。
3. 实际运行的验证及结果。
4. 未解决风险、阻塞和建议的下一张任务卡。

不要重复长篇项目背景，也不要粘贴完整命令日志。
